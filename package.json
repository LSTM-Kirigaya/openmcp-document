{
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "postbuild": "cp -r images .vitepress/dist/images",
    "preview": "vitepress preview"
  },
  "dependencies": {
    "vitepress": "^1.6.3"
  }
}
