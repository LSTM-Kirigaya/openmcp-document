# `gateway` Command

```bash
openmcp-cli gateway run
openmcp-cli gateway start
openmcp-cli gateway stop
openmcp-cli gateway restart
openmcp-cli gateway status
```

Common option:

- `-p, --port <port>` (default `8282`)
